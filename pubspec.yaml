name: flutter_base
description: A new base Flutter project.
version: 1.4.0
author: archko
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  json_annotation: 3.0.1

  dio: 3.0.10
  pull_to_refresh: ^1.6.1

  isolate: ^2.0.3

  provider: 4.3.2
  #webview_flutter: ^0.3.19+5
  flutter_webview_plugin: ^0.3.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: 3.4.0

# The following section is specific to Flutter.
flutter:
  plugin:
    androidPackage: cn.archko.flutter.base.flutter_base
    pluginClass: FlutterBasePlugin
